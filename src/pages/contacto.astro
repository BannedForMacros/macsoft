---
import Layout from '../layouts/Layout.astro';
---
<Layout
  title="¡Transformemos tu Idea en Realidad! | MacSoft"
  description="🚀 ¿Listo para el siguiente nivel? Contáctanos AHORA y recibe tu propuesta personalizada en menos de 24 horas. ¡Tu éxito nos motiva!"
>
  <style>
    /* ─────── Variables de colores ─────── */

    /* ─────── Animaciones avanzadas ─────── */
    @keyframes urgentPulse {
      0%, 100% { transform: scale(1); box-shadow: 0 0 0 0 rgba(6,182,212,0.7); }
      50% { transform: scale(1.05); box-shadow: 0 0 0 10px rgba(6,182,212,0); }
    }
    
    @keyframes magneticHover {
      0% { transform: translateY(0) rotate(0deg); }
      25% { transform: translateY(-2px) rotate(1deg); }
      50% { transform: translateY(-4px) rotate(0deg); }
      75% { transform: translateY(-2px) rotate(-1deg); }
      100% { transform: translateY(0) rotate(0deg); }
    }
    
    @keyframes slideInLeft {
      from { opacity: 0; transform: translateX(-100px); }
      to { opacity: 1; transform: translateX(0); }
    }
    
    @keyframes slideInRight {
      from { opacity: 0; transform: translateX(100px); }
      to { opacity: 1; transform: translateX(0); }
    }
    
    @keyframes countUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    
    @keyframes typewriter {
      from { width: 0; }
      to { width: 100%; }
    }
    
    @keyframes blink {
      0%, 50% { border-color: transparent; }
      51%, 100% { border-color: var(--accent); }
    }

    /* ─────── Utilidades ─────── */
    .animate-on-scroll {
      opacity: 0;
      transform: translateY(50px);
      transition: all 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    }
    .animate-on-scroll.in {
      opacity: 1;
      transform: translateY(0);
    }

    /* ─────── Hero épico ─────── */
    .hero-section {
      min-height: 100vh;
      background: linear-gradient(135deg, var(--primary) 0%, var(--accent-dark) 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      position: relative;
      overflow: hidden;
    }
    
    .hero-bg-animation {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 1000"><defs><radialGradient id="a" cx="50%" cy="50%" r="50%"><stop offset="0%" style="stop-color:rgba(6,182,212,0.1)"/><stop offset="100%" style="stop-color:rgba(6,182,212,0)"/></radialGradient></defs><circle cx="200" cy="200" r="100" fill="url(%23a)"><animate attributeName="r" values="100;150;100" dur="4s" repeatCount="indefinite"/></circle><circle cx="800" cy="300" r="80" fill="url(%23a)"><animate attributeName="r" values="80;120;80" dur="3s" repeatCount="indefinite"/></circle><circle cx="600" cy="700" r="120" fill="url(%23a)"><animate attributeName="r" values="120;180;120" dur="5s" repeatCount="indefinite"/></circle></svg>') no-repeat center center;
      background-size: cover;
      animation: float 20s ease-in-out infinite;
    }
    
    .hero-content {
      max-width: 800px;
      margin: 0 auto;
      padding: 2rem;
      position: relative;
      z-index: 2;
    }
    
    .hero-title {
      font-size: clamp(2.5rem, 5vw, 4rem);
      font-weight: 900;
      color: white;
      margin-bottom: 1rem;
      line-height: 1.2;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }
    
    .hero-subtitle {
      font-size: clamp(1.2rem, 3vw, 1.5rem);
      color: var(--accent-light);
      margin-bottom: 2rem;
      font-weight: 300;
    }
    
    .hero-cta {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 1rem 2.5rem;
      background: linear-gradient(45deg, var(--accent), var(--accent-light));
      color: white;
      font-weight: 700;
      font-size: 1.25rem;
      border-radius: 50px;
      text-decoration: none;
      box-shadow: 0 10px 30px rgba(6,182,212,0.4);
      transition: all 0.3s ease;
      animation: urgentPulse 3s ease-in-out infinite;
    }
    
    .hero-cta:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 40px rgba(6,182,212,0.6);
      animation: magneticHover 1s ease-in-out infinite;
    }

    /* ─────── Sección de urgencia ─────── */
    .urgency-bar {
      background: linear-gradient(90deg, var(--warning), #f97316);
      color: white;
      padding: 1rem;
      text-align: center;
      font-weight: 600;
      position: relative;
      overflow: hidden;
    }
    
    .urgency-bar::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
      animation: shine 3s ease-in-out infinite;
    }
    
    @keyframes shine {
      0% { left: -100%; }
      100% { left: 100%; }
    }

    /* ─────── Estadísticas sociales ─────── */
    .social-proof {
      padding: 4rem 2rem;
      background: var(--neutral-50);
    }
    
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 2rem;
      max-width: 1000px;
      margin: 0 auto;
    }
    
    .stat-card {
      background: white;
      padding: 2rem;
      border-radius: 20px;
      text-align: center;
      box-shadow: 0 10px 30px rgba(0,0,0,0.1);
      transition: transform 0.3s ease;
    }
    
    .stat-card:hover {
      transform: translateY(-10px);
    }
    
    .stat-number {
      font-size: 3rem;
      font-weight: 900;
      color: var(--accent);
      margin-bottom: 0.5rem;
      display: block;
    }
    
    .stat-label {
      font-size: 1.1rem;
      color: var(--neutral-700);
      font-weight: 600;
    }

    /* ─────── Beneficios persuasivos ─────── */
    .benefits-section {
      padding: 6rem 2rem;
      background: linear-gradient(135deg, var(--primary) 0%, var(--neutral-800) 100%);
      color: white;
    }
    
    .benefits-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
      max-width: 1200px;
      margin: 0 auto;
    }
    
    .benefit-card {
      background: rgba(255,255,255,0.1);
      backdrop-filter: blur(10px);
      padding: 2rem;
      border-radius: 20px;
      border: 1px solid rgba(255,255,255,0.2);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    
    .benefit-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 20px 40px rgba(6,182,212,0.2);
    }
    
    .benefit-icon {
      font-size: 3rem;
      color: var(--accent-light);
      margin-bottom: 1rem;
    }
    
    .benefit-title {
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
    }
    
    .benefit-description {
      color: var(--neutral-200);
      line-height: 1.6;
    }

    /* ─────── Formulario mejorado ─────── */
    .contact-section {
      padding: 6rem 2rem;
      background: var(--neutral-50);
    }
    
    .contact-container {
      max-width: 1200px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
      align-items: center;
    }
    
    .contact-info {
      padding: 2rem;
    }
    
    .contact-form {
      background: white;
      padding: 3rem;
      border-radius: 20px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.1);
      position: relative;
      overflow: hidden;
    }
    
    .contact-form::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 5px;
      background: linear-gradient(90deg, var(--accent), var(--accent-light));
    }
    
    .form-group {
      margin-bottom: 2rem;
      position: relative;
    }
    
    .form-label {
      display: block;
      margin-bottom: 0.5rem;
      font-weight: 600;
      color: var(--primary);
      font-size: 1.1rem;
    }
    
    .form-input,
    .form-textarea {
      width: 100%;
      padding: 1rem 1.5rem;
      border: 2px solid var(--neutral-200);
      border-radius: 15px;
      font-size: 1rem;
      transition: all 0.3s ease;
      background: var(--neutral-50);
    }
    
    .form-input:focus,
    .form-textarea:focus {
      outline: none;
      border-color: var(--accent);
      box-shadow: 0 0 0 3px rgba(6,182,212,0.1);
      background: white;
      transform: translateY(-2px);
    }
    
    .form-textarea {
      min-height: 120px;
      resize: vertical;
    }
    
    .form-submit {
      width: 100%;
      padding: 1.25rem;
      background: linear-gradient(45deg, var(--accent), var(--accent-light));
      color: white;
      font-weight: 700;
      font-size: 1.2rem;
      border: none;
      border-radius: 15px;
      cursor: pointer;
      transition: all 0.3s ease;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
    }
    
    .form-submit:hover {
      transform: translateY(-3px);
      box-shadow: 0 15px 30px rgba(6,182,212,0.4);
    }
    
    .form-submit:disabled {
      opacity: 0.7;
      cursor: not-allowed;
    }

    /* ─────── Elementos flotantes persuasivos ─────── */
    .floating-elements {
      position: fixed;
      z-index: 1000;
    }
    
    .floating-cta {
      bottom: 2rem;
      right: 2rem;
      width: 70px;
      height: 70px;
      background: linear-gradient(45deg, var(--accent), var(--accent-light));
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      box-shadow: 0 10px 30px rgba(6,182,212,0.4);
      animation: urgentPulse 2s ease-in-out infinite;
      transition: transform 0.3s ease;
    }
    
    .floating-cta:hover {
      transform: scale(1.1);
    }
    
    .floating-cta ion-icon {
      font-size: 2rem;
      color: white;
    }
    
    .notification-popup {
      bottom: 8rem;
      right: 2rem;
      background: white;
      padding: 1rem 1.5rem;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.2);
      max-width: 300px;
      opacity: 0;
      transform: translateY(20px);
      animation: slideInUp 0.5s ease forwards;
    }
    
    @keyframes slideInUp {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    .notification-popup.show {
      animation: slideInUp 0.5s ease forwards;
    }
    
    .notification-popup.hide {
      animation: slideInUp 0.5s ease reverse forwards;
    }

    /* ─────── Responsive ─────── */
    @media (max-width: 768px) {
      .contact-container {
        grid-template-columns: 1fr;
        gap: 2rem;
      }
      
      .stats-grid {
        grid-template-columns: repeat(2, 1fr);
      }
      
      .benefits-grid {
        grid-template-columns: 1fr;
      }
      
      .floating-cta {
        width: 60px;
        height: 60px;
      }
      
      .notification-popup {
        right: 1rem;
        max-width: 250px;
      }
    }

    /* ─────── Indicador de progreso ─────── */
    .progress-indicator {
      position: fixed;
      top: 0;
      left: 0;
      width: 0%;
      height: 4px;
      background: linear-gradient(90deg, var(--accent), var(--accent-light));
      z-index: 1001;
      transition: width 0.3s ease;
    }
  </style>

  <!-- Indicador de progreso -->
  <div class="progress-indicator" id="progressBar"></div>

  <!-- Barra de urgencia -->
    <div class="urgency-bar animate-on-scroll">
    ⚡ <strong>OFERTA ESPECIAL:</strong> Respuesta garantizada en 24 horas + Consulta GRATIS por tiempo limitado
    </div>

  <!-- Hero épico -->
  <section class="hero-section">
    <div class="hero-bg-animation"></div>
    <div class="hero-content animate-on-scroll">
      <h1 class="hero-title">
        ¿Listo para <span style="color: var(--accent-light);">TRANSFORMAR</span> tu negocio?
      </h1>
      <p class="hero-subtitle">
        Únete a más de 500+ empresas que ya confiaron en nosotros. 
        Tu proyecto merece lo mejor, y estamos aquí para hacerlo realidad.
      </p>
      <a href="#contacto-form" class="hero-cta">
        <ion-icon name="rocket-outline"></ion-icon>
        ¡EMPEZAR AHORA!
      </a>
    </div>
  </section>

  <!-- Prueba social -->
  <section class="social-proof">
    <div class="stats-grid">
      <div class="stat-card animate-on-scroll">
        <span class="stat-number" data-count="20">0</span>
        <span class="stat-label">Proyectos Exitosos</span>
      </div>
      <div class="stat-card animate-on-scroll">
        <span class="stat-number" data-count="24">0</span>
        <span class="stat-label">Horas Respuesta</span>
      </div>
      <div class="stat-card animate-on-scroll">
        <span class="stat-number" data-count="98">0</span>
        <span class="stat-label">% Satisfacción</span>
      </div>
      <div class="stat-card animate-on-scroll">
        <span class="stat-number" data-count="5">0</span>
        <span class="stat-label">Años Experiencia</span>
      </div>
    </div>
  </section>

  <!-- Beneficios persuasivos -->
  <section class="benefits-section">
    <div class="container mx-auto">
      <h2 class="text-4xl font-bold text-center mb-12 animate-on-scroll">
        ¿Por qué elegir MacSoft?
      </h2>
      <div class="benefits-grid">
        <div class="benefit-card animate-on-scroll">
          <ion-icon name="flash-outline" class="benefit-icon"></ion-icon>
          <h3 class="benefit-title">Respuesta Inmediata</h3>
          <p class="benefit-description">
            No pierdas tiempo esperando. Respuesta garantizada en menos de 24 horas
            con propuesta personalizada incluida.
          </p>
        </div>
        <div class="benefit-card animate-on-scroll">
          <ion-icon name="shield-checkmark-outline" class="benefit-icon"></ion-icon>
          <h3 class="benefit-title">Garantía Total</h3>
          <p class="benefit-description">
            Tu inversión está protegida. Si no estás 100% satisfecho, 
            trabajamos hasta que lo estés o te devolvemos tu dinero.
          </p>
        </div>
        <div class="benefit-card animate-on-scroll">
          <ion-icon name="trending-up-outline" class="benefit-icon"></ion-icon>
          <h3 class="benefit-title">Resultados Medibles</h3>
          <p class="benefit-description">
            No solo creamos, transformamos. Nuestros clientes ven resultados
            reales desde el primer día.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Formulario principal -->
  <section id="contacto-form" class="contact-section">
    <div class="contact-container">
      <div class="contact-info animate-on-scroll">
        <h2 class="text-4xl font-bold mb-6 text-primary">
          ¡Tu éxito está a un clic de distancia!
        </h2>
        <p class="text-xl mb-8 text-neutral-700">
          Completa el formulario y recibe una propuesta personalizada 
          en menos de 24 horas. ¡Es completamente GRATIS!
        </p>
        
        <div class="space-y-6">
          <div class="flex items-center gap-4">
            <div class="w-12 h-12 bg-accent text-white rounded-full flex items-center justify-center">
              <ion-icon name="call-outline"></ion-icon>
            </div>
            <div>
              <h3 class="font-bold text-lg">Llámanos Directo</h3>
              <p class="text-accent font-semibold">+51 936 313 648</p>
            </div>
          </div>
          
          <div class="flex items-center gap-4">
            <div class="w-12 h-12 bg-accent text-white rounded-full flex items-center justify-center">
              <ion-icon name="mail-outline"></ion-icon>
            </div>
            <div>
              <h3 class="font-bold text-lg">Email Directo</h3>
              <p class="text-accent font-semibold">cjrmac0706@gmail.com</p>
            </div>
          </div>
          
          <div class="flex items-center gap-4">
            <div class="w-12 h-12 bg-success text-white rounded-full flex items-center justify-center">
              <ion-icon name="time-outline"></ion-icon>
            </div>
            <div>
              <h3 class="font-bold text-lg">Disponibilidad</h3>
              <p class="text-success font-semibold">24/7 - Respuesta garantizada</p>
            </div>
          </div>
        </div>
      </div>
      
      <div class="contact-form animate-on-scroll">
        <h3 class="text-2xl font-bold mb-6 text-center text-primary">
          🚀 ¡Empezar es GRATIS!
        </h3>
        
        <form id="contactForm">
          <div class="form-group">
            <label for="name" class="form-label">Nombre Completo *</label>
            <input type="text" id="name" name="name" class="form-input" 
                   placeholder="¿Cómo te llamas?" required>
          </div>
          
          <div class="form-group">
            <label for="email" class="form-label">Email *</label>
            <input type="email" id="email" name="email" class="form-input" 
                   placeholder="tu@empresa.com" required>
          </div>
          
          <div class="form-group">
            <label for="phone" class="form-label">Teléfono</label>
            <input type="tel" id="phone" name="phone" class="form-input" 
                   placeholder="+51 999 999 999">
          </div>
          
          <div class="form-group">
            <label for="company" class="form-label">Empresa</label>
            <input type="text" id="company" name="company" class="form-input" 
                   placeholder="Nombre de tu empresa">
          </div>
          
          <div class="form-group">
            <label for="budget" class="form-label">Presupuesto Estimado</label>
            <select id="budget" name="budget" class="form-input">
              <option value="">Selecciona un rango</option>
              <option value="1000-5000">$1,000 - $5,000</option>
              <option value="5000-10000">$5,000 - $10,000</option>
              <option value="10000-25000">$10,000 - $25,000</option>
              <option value="25000+">$25,000+</option>
            </select>
          </div>
          
          <div class="form-group">
            <label for="message" class="form-label">Cuéntanos tu proyecto *</label>
            <textarea id="message" name="message" class="form-textarea" 
                      placeholder="¿Qué necesitas? ¿Cuál es tu objetivo? ¿Tienes alguna fecha límite?" required></textarea>
          </div>
          
          <button type="submit" class="form-submit">
            <ion-icon name="send-outline"></ion-icon>
            <span>¡ENVIAR MI PROYECTO!</span>
          </button>
        </form>
      </div>
    </div>
  </section>

  <!-- Elementos flotantes -->
  <div class="floating-elements">
    <div class="floating-cta" id="floatingCta">
      <ion-icon name="chatbubble-ellipses-outline"></ion-icon>
    </div>
    
    <div class="notification-popup" id="notificationPopup">
      <div class="flex items-center gap-3">
        <div class="w-8 h-8 bg-success text-white rounded-full flex items-center justify-center">
          <ion-icon name="checkmark-outline"></ion-icon>
        </div>
        <div>
          <p class="font-semibold text-sm">¡Alguien acaba de contactarnos!</p>
          <p class="text-xs text-neutral-600">Únete a nuestros clientes satisfechos</p>
        </div>
      </div>
    </div>
  </div>


<!-- Scripts -->
<script>
// ────────────────────────────────────────────────────────────────
// @ts-nocheck
document.addEventListener('DOMContentLoaded', () => {
  // 1) Animar contadores
  function animateCounter(el) {
    const countTo = parseInt(el.getAttribute('data-count') || '0', 10);
    const duration = 2000;
    const stepTime = countTo > 0 ? Math.max(50, duration / countTo) : duration;
    let count = 0;

    const timer = setInterval(() => {
      count++;
      el.textContent = count.toString();
      if (count >= countTo) clearInterval(timer);
    }, stepTime);

    el.style.opacity = '1';
    el.style.transform = 'translateY(0)';
    el.style.animation = 'countUp 0.5s ease forwards';
  }

  // 2) Intersection Observer para scroll + contadores
  const io = new IntersectionObserver((entries, obs) => {
    entries.forEach(entry => {
      if (!entry.isIntersecting) return;
      entry.target.classList.add('in');
      entry.target.querySelectorAll('.stat-number').forEach(animateCounter);
      obs.unobserve(entry.target);
    });
  }, { threshold: 0.1, rootMargin: '0px 0px -50px 0px' });

  document.querySelectorAll('.animate-on-scroll, .stat-card').forEach(el => io.observe(el));

  // 3) Barra de progreso
  function updateProgressBar() {
    const bar = document.getElementById('progressBar');
    if (!bar) return;
    const scrollTop = document.documentElement.scrollTop;
    const height   = document.documentElement.scrollHeight - document.documentElement.clientHeight;
    const pct      = (scrollTop / height) * 100;
    bar.style.width = pct + '%';
  }
  window.addEventListener('scroll', updateProgressBar);

  // 4) Notificación periódica
  function showNotification() {
    const popup = document.getElementById('notificationPopup');
    if (!popup) return;
    popup.classList.add('show');
    setTimeout(() => {
      popup.classList.remove('show');
      popup.classList.add('hide');
    }, 4000);
    setTimeout(() => popup.classList.remove('hide'), 4500);
  }
  setTimeout(showNotification, 5000);
  setInterval(showNotification, 30000);

  // 5) Floating CTA
  const floatingCta = document.getElementById('floatingCta');
  if (floatingCta) {
    floatingCta.addEventListener('click', () => {
      const form = document.getElementById('contacto-form');
      if (form) form.scrollIntoView({ behavior: 'smooth', block: 'start' });
    });
  }

  // 6) Exit-intent modal
  let exitShown = false;
  document.addEventListener('mouseleave', e => {
    if (e.clientY <= 0 && !exitShown) {
      exitShown = true;
      showExitIntentPopup();
    }
  });
  function showExitIntentPopup() {
    // no duplicar
    if (document.querySelector('.exit-intent-overlay')) return;
    const overlay = document.createElement('div');
    overlay.className = 'exit-intent-overlay';
    overlay.innerHTML = `
      <div class="exit-intent-modal">
        <button class="exit-intent-close">×</button>
        <h2>¡Espera! 🚀</h2>
        <p>Antes de irte, cuéntanos tu proyecto. <strong>Es gratis y sin compromiso.</strong></p>
        <button class="exit-intent-action">¡Quiero mi consulta gratis!</button>
      </div>
    `;
    document.body.appendChild(overlay);
    overlay.querySelector('.exit-intent-close')?.addEventListener('click', () => overlay.remove());
    overlay.querySelector('.exit-intent-action')?.addEventListener('click', () => {
      overlay.remove();
      const form = document.getElementById('contacto-form');
      if (form) form.scrollIntoView({ behavior: 'smooth', block: 'start' });
    });
  }


});
</script>

<!-- CSS necesario para el modal y banner fijo -->
<!-- 2) El CSS imprescindible para que se vea -->
<style is:global>
  /* ── Salen tal cual al navegador, no scoped ── */
  .exit-intent-overlay {
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    background: rgba(0,0,0,0.7);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10000;
  }
  .exit-intent-modal {
    position: relative;
    background: white;
    padding: 2rem;
    border-radius: 0.5rem;
    max-width: 400px;
    width: 90%;
    text-align: center;
    box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    animation: popIn 0.4s ease;
  }
  .exit-intent-close {
    position: absolute;
    top: 0.5rem; right: 0.5rem;
    background: none; border: none;
    font-size: 1.5rem; cursor: pointer;
  }
  .exit-intent-action {
    margin-top: 1rem;
    padding: 0.75rem 1.5rem;
    background: #06b6d4; color: white;
    border: none; border-radius: 0.5rem;
    cursor: pointer; font-weight: bold;
  }

  @keyframes popIn {
    from { opacity: 0; transform: scale(0.8); }
    to   { opacity: 1; transform: scale(1);   }
  }

  /* Y tu shimmer de la barra de urgencia debajo del texto: */
  .urgency-bar::before {
    z-index: -1;
    pointer-events: none;
  }
</style>




  <!-- Agregar estilos para las animaciones adicionales -->
  <style>
    @keyframes slideDown {
      from {
        transform: translateY(-100%);
      }
      to {
        transform: translateY(0);
      }
    }
    
    @keyframes popIn {
      from {
        opacity: 0;
        transform: scale(0.5);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }
  </style>
</Layout>